import { FUNCTIONS } from './functions'

export const DEFAULTS = {
  lineColor: '#FFFFFF',
  rulersColor: '#111111',
  func: FUNCTIONS.SIN,
  interval: 1,
  amplitude: 80,
  speed: 100,
  showRulers: true,
  autoStop: false
}